# Big / re-downloadable — do NOT commit (re-fetch on the H100, see H100_HANDOFF.md)
checkpoints/
# everything else here (results, scripts, configs, patches, memory copy, small
# eval videos ~33M, mimicgen_src ~83M, weakregion reports) is fine to commit.

__pycache__/
*.pyc
# phase1 demo pools are large + regenerable (record_pi0_demos.py)
phase1_data/*.hdf5

# generated LeRobot training data (large, regenerable)
**/lerobot/
phase1_data/
# rollout videos (incidental)
weakregion/**/media/

# Claude Code local config
.claude/
weakregion/emb_*.npy

# large regenerable gradient/embedding artifacts (recreate: influence_score.py sketch / failretr.py)
weakregion/rcless/
weakregion/rcless_raw/
weakregion/failstates/
weakregion/failretr_demo_emb.npy

# run logs (training/eval/orchestration)
weakregion/*.log
weakregion/**/serve.log

# talk: rollout frame dumps are large + regenerable (make_filmstrip.py)
talk/rollout_frames/

# collaborator's CIFAR sandbox — standalone clone of RansML/xgradtest, not part of this repo
xgradtest/

# bandit_v1 ledger: regenerable snapshot/event parquets (hashes.json IS committed)
bandit_v1/ledger/*.parquet

# bandit_v1 saved-state captures (state.npz/model.xml/ep_meta.json/fingerprint.json
# per start dir): deterministically regenerable from a fixed seed via
# states.capture_start. Discarded scanned seeds are kept for resume, up to
# 2000 scan dirs ~= ~1GB. The frozen tercile map
# (bandit_v1/ledger/diag_tercile_map.json, NOT under ledger/*.parquet above)
# and diagnosis.py's code are what's committed; the captured states themselves
# are not.
bandit_v1/states/

# ---- 2026-08-24 cleanup: code + reports tracked, bulky data artifacts not ----

# paper / talk heavy media (talk scripts + narrative md ARE tracked)
main (1).tex
refs.bib
runB.log
talk/figs/
talk/*.pptx
talk/*.pdf
weakregion/daily_report_*.pdf

# gradient_analysis + sandboxes: gradient sketches, absorption ladders,
# trajectory dumps, synthetic result dumps -- large + regenerable from drivers
gradient_analysis/**/*.npz
gradient_analysis/**/*.npy
gradient_analysis/**/*.pt
gradient_analysis/**/*.parquet
gradient_analysis/**/*.log
gradient_analysis/**/sketches_*/
gradient_analysis/q2_absorption/
gradient_analysis/gate_traj/
gradient_analysis/llm_borrow/f4_synth*.json
gradient_analysis/llm_borrow/part1_synthetic_robot.json
cifar_experiments/sandbox_regions/**/*.npz
cifar_experiments/sandbox_regions/**/*.pt
cifar_experiments/sandbox_regions/**/*.parquet
prescription_compare/**/*.log

# factor analysis: plots/renders regenerable from scripts/
weakregion/factor_analysis/plots/
weakregion/factor_analysis/renders/
weakregion/factor_analysis/*.html

# bandit_v1 track-2 (ppccab) tree: same conventions as the main ledger
bandit_v1/states_ppccab/
bandit_v1/ledger_ppccab/*.parquet
bandit_v1/ledger*/pull_logs/
bandit_v1/ledger*/*.lock
bandit_v1/ledger*/map_models.joblib
